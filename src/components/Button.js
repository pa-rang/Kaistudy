import React from "react"

import styled from "styled-components"

const StyledButton = styled.button`
	width: auto;
	min-width: 150px;
	color: white;
	
`

const Button = (props) => {
	return (
		<StyledButton >

		</StyledButton>
	)
}
